<div class="comment-form">
  <%= form_with(model: [practice_diary, practice_comment]) do |form| %>
    <div id="errors_area">
      <%= render partial: 'practice_comments/error' %>
    </div>
    <div class="panel panel-info">
      <div class="panel-heading">
        <p>コメントを投稿する</p>
      </div>
      <div class="panel-body">
        <div class="input-field">
          <p><%= form.label :practice_comment_content, class: "active" %></p>
          <%= form.text_area :practice_comment_content, class: "aterialize-textarea", placeholder: "150文字以内で入力して下さい。" %>
        </div>
        <div class="actions">
          <%= form.submit %>
        </div>
      </div>
    </div>
  <% end %>
</div>